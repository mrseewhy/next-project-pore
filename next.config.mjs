/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // Optional: Configure base path if your site won't be at the root
  // basePath: '/your-base-path',
  // Optional: Required if your site will be in a subdirectory
  trailingSlash: true,
};

export default nextConfig;
